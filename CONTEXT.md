# 媒体去重系统

本上下文统一描述中心端与各媒体节点协作完成媒体扫描、特征计算、同步和受控删除时使用的领域词汇。

## Language

**媒体节点**：
部署 Agent、Worker，并可选部署删除 Helper 的一台本地媒体计算机。
_Avoid_: 客户端、Worker 节点

**节点托盘程序**：
常驻媒体节点通知区域、供本机操作员配置和管理节点组件的应用程序；它不执行媒体计算或删除。
_Avoid_: 节点 GUI、Worker 管理器

**节点控制台**：
节点托盘程序显示状态、配置和操作入口的可见主窗口。
_Avoid_: 中央 GUI、管理后台

**Agent**：
媒体节点上的协调组件，负责扫描、同步以及 Worker 的生命周期和任务调度。
_Avoid_: 媒体 Worker、托盘服务

**机器唯一 ID**：
Agent、Helper 和节点托盘程序根据本机 CPU ID、主板 ID 和 Windows 系统 ID 按同一规则自动计算的稳定节点身份；Agent 在握手时向 GUI 上报，GUI 只能发现和使用它，不能配置或覆盖。
_Avoid_: 专属 machine ID、GUI 节点名称、Agent 地址

**Agent endpoint**：
GUI 用于连接 Agent 的网络地址；它只描述“到哪里连接”，实际机器身份由 Agent 握手上报。
_Avoid_: 机器唯一 ID、节点身份

**Worker**：
由 Agent 创建并管理、负责媒体特征计算的子进程；操作员不能绕过 Agent 单独启停它。
_Avoid_: Agent、计算节点

**删除 Helper**：
媒体节点上可选部署、以管理员权限执行受控本地删除的独立组件。
_Avoid_: Helper、删除节点、Agent Helper

**自动启动**：
节点托盘程序按已保存策略在用户登录后启动对应组件；不表示组件失败后无限循环重启。
_Avoid_: Windows 服务、崩溃循环

**手动启动**：
对应组件只在操作员从节点控制台或托盘菜单明确发起操作后启动。
_Avoid_: 禁用
