# Rust V2 媒体去重系统

本上下文统一描述管理端与媒体节点完成扫描、特征计算、同步、多机器去重和受控删除时使用的领域词汇。

## 参与者

**管理端**：
连接一个或多个媒体节点、编排同步和跨机器分析并展示结果的操作端。
_Avoid_: 中心节点、Agent GUI、节点控制台

**媒体节点**：
拥有本机媒体文件与本地事实，负责扫描、计算、本地分析、预览和删除的一台计算机。
_Avoid_: Agent、Worker 节点、客户端

**Worker**：
由媒体节点管理、只负责媒体读取和特征计算的隔离执行者。
_Avoid_: 媒体节点、Agent、调度器

**节点端点**：
管理端用于连接媒体节点的网络地址；它不等同于媒体节点的稳定身份。
_Avoid_: 机器唯一 ID、节点身份

## 任务与生命周期

**运行任务**：
当前进程内一次扫描、计算、分析、同步或删除操作及其即时状态；进程结束后不再存在。
_Avoid_: 持久任务、可恢复任务、任务历史

**完成扫描快照**：
一次成功扫描在当前媒体节点进程内保留的成功文件清单、扫描范围和版本门禁；普通同步不会改变它，文件库发生新修订后不能用于开始新分析。
_Avoid_: 扫描历史、同步快照、可恢复任务

**任务恢复**：
进程重启后继续未完成任务的能力；Rust V2 明确不支持，用户重新发起操作。
_Avoid_: Worker 替换、同步重放

**当前运行崩溃隔离**：
同一次运行中 Worker 崩溃只影响当前项，并由媒体节点补充可用 Worker 的能力。
_Avoid_: 任务恢复、断点续算

## 数据与同步

**当前文件事实**：
媒体节点当前已知的文件位置、内容身份和有效状态，是扫描收尾、分析输入与删除结果的共同依据。
_Avoid_: 扫描日志、删除历史

**媒体缓存**：
由内容身份索引、可在后续扫描和分析中复用的完整媒体元数据与特征。
_Avoid_: 任务结果占位、运行进度

**文件库修订**：
当前文件事实集合的单调版本；成功扫描收尾或成功删除会产生新修订，普通同步和 UI 查询不会。
_Avoid_: 同步游标、任务阶段

**同步一致性**：
保证媒体节点的已提交当前事实最终完整到达中心事实库的约束，包括重放、确认、游标和全量替换。
_Avoid_: 任务恢复、分析恢复

## 分析与结果

**本地分析结果**：
媒体节点根据本机完成扫描快照生成的去重结果；只保留最近一次成功版本。
_Avoid_: 跨机器分析结果、运行中候选

**跨机器分析结果**：
管理端根据已经同步的多个媒体节点事实生成并自行拥有的去重结果。
_Avoid_: 节点本地分析结果、同步结果

**删除队列**：
一次删除操作中按顺序执行并逐项复核的临时文件集合；它不是历史，也不跨进程继续。
_Avoid_: 删除批次历史、删除恢复日志
