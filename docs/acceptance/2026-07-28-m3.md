# M3 First-Screen Acceptance — 2026-07-28

## Result

M3 Task 8 small PostgreSQL acceptance and Task 9 million-scale acceptance are
PASS on the primary Windows host.

The final post-review controller run is `20260728-095748-517-a8109940`;
its machine-readable evidence is
`.superpowers/evidence/m3-20260728-095748-517-a8109940/m3-evidence.json`.
All 14 required gates passed after the broad independent review and both
format-gate review fixes. It is the final M3 completion authority.

- Final scale run 1: `20260728-083749-043-c619b0ed`
- Evidence:
  `.superpowers/evidence/m3-20260728-083749-043-c619b0ed/m3-evidence.json`
- Final scale run 2: `20260728-084011-444-41524c8c`
- Evidence:
  `.superpowers/evidence/m3-20260728-084011-444-41524c8c/m3-evidence.json`
- PostgreSQL: `server_version_num=160014`
- Second independent Windows host: **USER_WAIVED** (not executed).

Each fresh verifier run passed, in order: small PostgreSQL acceptance,
`scale_seed`, `scale_reuse`, full-repository unit tests, M3 race tests, and
full-repository `go vet`. Every gate has a real evidence-directory log; an
empty successful `vet.log` is retained as a zero-byte file.

## Task 10 final-gate candidate

Task 9 authority remains the two runs above. Task 10 produced a separate
candidate for root broad review; it does not replace those authority runs and
does not by itself mark the M3 checklist complete.

- Expanded quick run: `20260728-091430-819-e2aef690`
- Quick evidence:
  `.superpowers/evidence/m3-20260728-091430-819-e2aef690/m3-evidence.json`
- Review I1 quick run: `20260728-094000-216-7662cfcc`
- Review I1 evidence:
  `.superpowers/evidence/m3-20260728-094000-216-7662cfcc/m3-evidence.json`
- Review I1 Round2 quick run: `20260728-095201-610-c96d87ff`
- Review I1 Round2 evidence:
  `.superpowers/evidence/m3-20260728-095201-610-c96d87ff/m3-evidence.json`
- Complete `-RunScale` candidate: `20260728-091611-043-0c8f007a`
- Candidate evidence:
  `.superpowers/evidence/m3-20260728-091611-043-0c8f007a/m3-evidence.json`

The complete candidate recorded 14 dynamic required gates, all PASS:
repository-owned formatting, pure-Go full tests, CGO full unit tests, M3 race,
full vet, 10 explicit PostgreSQL contracts, strict small acceptance, the
12/23/7 marker matrices, scale seed/reuse, schema/index audit, and cleanup
audit. The summary was written before one line per gate and the unique final
PASS line.

Review I1 found that the first formatting implementation enumerated only the
repository root, `cmd/`, `internal/`, and `integration/`, missing the
repository-owned generator `testdata/m2/gen_corrupt.go`. The controller now
uses a tested safe full-repository helper, includes all 104 repository-owned Go
files, and rejects zero files, duplicates, and paths outside the repository.
It excludes `.git`, `.superpowers`, `.tmp` (including the local module cache),
`vendor`, and other dependency/build directories without using `rg` as a
runtime prerequisite. The I1 quick evidence records
`gofmt -l <repo-owned Go files count=104>`, a real zero-byte format log, all
12 quick gates PASS, and zero secret matches. Scale was not rerun because I1
changes only format-file enumeration; the complete scale candidate above
remains the scale evidence.

Review I1 Round2 added a real Windows junction regression. The pre-fix
validator accepted `root/junction/outside.go` even though the junction target
was physically outside the supplied root. The validator now walks every
root-to-file component, rejects any `FileAttributes.ReparsePoint`, and repeats
the component check before returning the complete list. Separate non-Go and
missing-file negatives bring the final controller helper matrix to 18 cases.
The Round2 quick run again records 104 formatted files, all 12 gates PASS, a
zero-byte format log, 18 PostgreSQL cleanup schemas at residual 0, and no
evidence secret matches. Scale was not rerun because this remains a format
path-validation-only correction.

The PostgreSQL contract evidence lists the exact 10 expected and passed
top-level tests. It records 18 unique scoped schemas (the rollback contract has
nine independent subcases), all with residual 0; exact definitions for
`idx_files_sha512_id`, `idx_dup_groups_kind`, and `idx_dup_members_file`;
an actual executed keyset plan; failure rollback, unknown-commit convergence,
and M4 preservation.

| Task 10 candidate process | Seed ms | Total ms | Image ms | Video ms | Peak HeapInuse bytes |
|---|---:|---:|---:|---:|---:|
| seed run 1 | 42,919 | 12,389 | 794 | 172 | 979,148,800 |
| seed run 2 | — | 13,479 | 818 | 172 | 971,046,912 |
| reuse | 0 | 13,157 | 802 | 173 | 968,851,456 |

Candidate evidence secret scan found zero DSN matches. Direct PostgreSQL
inspection found no candidate scale schema, `fs_t4_*`, or `m3_small_*`
residual. Small and scale reuse cleanup are both 0, and the scale reuse
full-lifecycle public snapshot is true.

## Deterministic dataset

Seed is exactly 1. SHA identities are canonical lowercase 128-character TEXT
values with separate image/video/exact domains. Fixed 50,000-row
`pgx.CopyFromSource` chunks reuse a single row buffer.

| Physical/semantic quantity | Exact result |
|---|---:|
| `files` | 1,350,000 |
| `image_features` | 1,000,000 |
| filtered image q=30 rows | 9,600 |
| loaded image features | 990,400 |
| `video_features` / loaded | 200,000 / 200,000 |
| image / video candidate pairs | 60,000 / 15,000 |
| exact groups / members | 50,000 / 150,000 |
| groups / members written | 125,000 / 300,000 |
| skipped pairs / bad rows | 0 / 0 |

The original `2+i%3` formula sums to 149,999 for groups 0 through 49,999.
The generator adds one copy only to group 49,999, preserving 50,000 exact
groups while producing the required 150,000 exact members.

Image band domains mathematically prevent random/cross-cluster candidates;
each four-member cluster contributes exactly `C(4,2)`. Video generation
checks each non-cluster PDQ against the bounded active two-second duration
window and reserves only each cluster's six close pairs.

## Measurements

All values are Analyzer-only measurements. HeapInuse is sampled immediately,
then every 50ms, and once more at stop.

| Run/process | Seed ms | Total ms | Image screen ms | Video screen ms | Peak HeapInuse bytes |
|---|---:|---:|---:|---:|---:|
| final 1 seed run 1 | 43,780 | 12,573 | 799 | 172 | 983,351,296 |
| final 1 seed run 2 | — | 13,577 | 830 | 173 | 982,958,080 |
| final 1 reuse | 0 | 12,745 | 806 | 173 | 1,120,428,032 |
| final 2 seed run 1 | 42,276 | 12,045 | 809 | 173 | 983,932,928 |
| final 2 seed run 2 | — | 13,126 | 830 | 174 | 983,416,832 |
| final 2 reuse | 0 | 12,989 | 858 | 173 | 982,802,432 |

Acceptance limits are image screen ≤5,000ms, video screen ≤3,000ms,
Analyzer total ≤90,000ms, and peak HeapInuse ≤4GiB. The unoptimized production
implementation passed the first real measurement; measurement-first policy
therefore produced no production algorithm change.

## PostgreSQL plans

All recorded plans use `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`, return
50,000 actual rows, and carry actual timing/buffer fields.

| Final run | Query | Root / index | Execution ms | Shared reads |
|---|---|---|---:|---:|
| 1 | files | Limit / `idx_files_sha512` | 61.791 | 2,649 |
| 1 | image | Limit / `image_features_pkey` | 18.326 | 2,400 |
| 1 | video | Limit / `video_features_pkey` | 17.619 | 2,376 |
| 2 | files | Limit / `idx_files_sha512` | 63.107 | 2,690 |
| 2 | image | Limit / `image_features_pkey` | 17.733 | 2,400 |
| 2 | video | Limit / `video_features_pkey` | 18.523 | 2,376 |

## Isolation, reuse, and cleanup

Each seed process took a complete public catalog baseline before CREATE.
The post-`central.sql` snapshot was only an intermediate assertion and could
not set the marker true. A final snapshot after both Analyzers and all DB
assertions covered the full seed lifecycle. Relations (including sequences),
columns, constraints, and indexes were unchanged.

Each reuse process took its own entry baseline, verified physical counts,
ran Analyzer and semantic assertions, dropped only the exact run-owned schema,
proved residual 0, and only then compared its final public snapshot. Both
seed/reuse markers in both authority runs report full-window public unchanged;
a direct PostgreSQL audit found zero residual final schemas.

Cleanup safety tests rejected `public`, accepted only the exact
`m3_scale_<run>` name, and proved idempotent missing-schema cleanup residual 0.
An unreachable-PostgreSQL `-Scale` run failed closed and restored PATH, CGO,
DSN, run/evidence, all scale environment variables, and current directory
including absent-versus-empty state.

## Strict evidence and security

- Task 8 marker negative matrix: 12 rejected malformed cases.
- Task 9 marker negative matrix: 23 rejected malformed/state/boundary cases,
  including authority-marker mutations.
- Cleanup marker negative matrix: 7 rejected missing/duplicate/wrong
  run/schema/null/string/nonzero cases.
- Scale markers strictly validate required/non-null/native JSON types, exact
  counts, positive total/peak/EXPLAIN execution, all six stages, performance
  limits, seed/reuse state, common run/schema, and final cleanup.
- Fallback cleanup emits one strict JSON marker; nonzero residual fails. If the
  primary gate and cleanup both fail, both reasons remain in the final error.
- Evidence secret scan: 0 DSN/password/token matches.
- Task 9 checkbox and progress ledger were not modified.

The previous authority runs `20260728-080953-232-d4fed447` and
`20260728-081525-795-10d619e8` are retained as historical evidence but are
**superseded**: their public marker covered only the seed post-DDL checkpoint
and reuse defaulted true, and their validator allowed zero total/heap/EXPLAIN
execution.
